import React from "react";
import css from "./home.module.css";

const Home = () => {
    return (
        <div className={css.wrapper}>
            <h1>Home js</h1>
        </div>
    );
};

export default Home;
